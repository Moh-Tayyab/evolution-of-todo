# @spec: specs/002-fullstack-web-app/plan.md
# Frontend Environment Variables Example
# Copy this to .env.local and configure with your actual values

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Better Auth
BETTER_AUTH_URL=http://localhost:3000/api/auth

# Shared with FastAPI backend (MUST be the same in both .env files)
# Get your Neon PostgreSQL connection string from https://console.neon.tech/
DATABASE_URL=postgresql://user:password@ep-cool-region.aws.neon.tech/dbname?sslmode=require

# Shared secret for JWT signing (must be same in backend .env)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-min-32-characters-long
