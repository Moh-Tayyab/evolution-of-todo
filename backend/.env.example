# =============================================================================
# Evolution of Todo - Backend Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# For Neon PostgreSQL (recommended for production)
# Get free database at: https://neon.tech
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# For local development with SQLite (alternative)
# DATABASE_URL=sqlite:///./todo.db

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# JWT Secret for token signing and verification
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-jwt-secret-here-min-32-characters-long

# Better Auth URL (frontend authentication service)
BETTER_AUTH_URL=http://localhost:3000

# Better Auth Secret (shared secret with frontend for JWT verification)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
BETTER_AUTH_SECRET=your-shared-secret-here

# -----------------------------------------------------------------------------
# AI Chatbot (OpenAI Integration)
# -----------------------------------------------------------------------------
# OpenAI API Key for AI chatbot functionality
# Required for Phase III: AI Chatbot feature
# Get your API key from: https://platform.openai.com/api-keys
# Models used: gpt-4o-mini (cost-effective, fast)
OPENAI_API_KEY=your-openai-api-key-here

# -----------------------------------------------------------------------------
# Optional: Rate Limiting
# -----------------------------------------------------------------------------
# Redis URL for distributed rate limiting (optional)
# If not set, falls back to in-memory rate limiting
# REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Set to "true" for debug mode (verbose logging, auto-reload)
DEBUG=false

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Port for the FastAPI server
PORT=8000
